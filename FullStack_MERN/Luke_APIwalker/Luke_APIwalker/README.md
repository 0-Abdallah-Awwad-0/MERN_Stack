# Luke APIwalker

React + Vite assignment using SWAPI, React Router, Axios, useState, useEffect, useParams, useNavigate, and Material UI.

## Run

Double-click `START_PROJECT.cmd` on Windows, or run:

```bash
npm install
npm run dev
```
